import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 12,
  },
  separator: {
    height: 10,
  },
  search: {
    borderWidth: 1,
    borderColor: 'grey',
    marginBottom: 12,
    paddingLeft: 8,
    borderRadius: 12,
  },
});
